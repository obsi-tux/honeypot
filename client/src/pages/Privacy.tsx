const sections = [{ title: "What we process", text: "The scanner receives the source text you intentionally submit, along with the filename and repository label you provide. The detector uses this information to identify likely credentials and return findings." }, { title: "What we retain", text: "The detector is designed to retain masked findings and scan metadata rather than raw secret values. A masked value is a redacted representation that cannot be used as the original credential." }, { title: "Local-first operation", text: "The live scanner in this project sends submitted text to the server process hosting the detector. It does not send source text to an external analytics or AI service as part of the scan." }, { title: "Your choices", text: "You can avoid submitting sensitive source text to the live scanner and run the supplied detector locally through its pre-commit hook. You may also contact the project owner to request clarification about a specific data flow." }];
export default function Privacy() { return <LegalPage eyebrow="Legal / privacy" title="Privacy policy" intro="A plain-language description of what the scanner sees, what it keeps, and where you remain in control.">{sections.map(section => <section key={section.title}><h2 className="display-font text-2xl font-semibold">{section.title}</h2><p className="mt-3 leading-8 text-muted-foreground">{section.text}</p></section>)}<section><h2 className="display-font text-2xl font-semibold">Updates</h2><p className="mt-3 leading-8 text-muted-foreground">This policy may change as the product moves from an MVP to a production service. Material changes should be reflected on this page with a new effective date.</p></section></LegalPage>; }
function LegalPage({ eyebrow, title, intro, children }: { eyebrow: string; title: string; intro: string; children: React.ReactNode }) { return <div className="container py-16 sm:py-24"><div className="max-w-3xl"><p className="eyebrow">{eyebrow}</p><h1 className="display-font mt-4 text-5xl font-bold sm:text-7xl">{title}</h1><p className="mt-7 text-lg leading-8 text-muted-foreground">{intro}</p><p className="mono-font mt-5 text-[11px] uppercase tracking-[.12em] text-muted-foreground">Effective September 15, 2026</p></div><article className="mt-16 max-w-3xl space-y-12 border-t hairline pt-12">{children}</article></div>; }
