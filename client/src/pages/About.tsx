import { ArrowRight, Eye, HeartHandshake, Scale, ShieldCheck } from "lucide-react";
import { Link } from "wouter";

export default function About() { return <div className="container py-16 sm:py-24"><div className="grid gap-14 lg:grid-cols-[.9fr_1.1fr]"><div><p className="eyebrow">About / why this exists</p><h1 className="display-font mt-4 text-5xl font-bold leading-[.98] sm:text-7xl">Make the safe path the easy path.</h1></div><div className="max-w-xl lg:pt-12"><p className="text-xl leading-9">Most secret scanners speak in alerts after the uncomfortable part has already happened. We prefer the merge point: early enough to change the code, close enough to the developer to explain why.</p><p className="mt-7 leading-7 text-muted-foreground">This product wraps the supplied Secret Leak Detector backend in an interface that respects the seriousness of the task. It keeps the signal legible, the data footprint small, and the next action obvious.</p></div></div><section className="mt-20 grid gap-px overflow-hidden border hairline bg-border md:grid-cols-3">{[[<Eye />, "Inspectability", "A developer should be able to understand the finding without trusting a mystery score."], [<Scale />, "Proportion", "The product should be quieter than the risk it is helping you avoid."], [<HeartHandshake />, "Respect", "Privacy is not a marketing panel. It is a design constraint in the data path."]].map(([icon, title, text]) => <article key={String(title)} className="surface p-8 sm:p-10"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-secondary text-primary">{icon}</span><h2 className="display-font mt-8 text-2xl font-semibold">{title}</h2><p className="mt-3 leading-7 text-muted-foreground">{text}</p></article>)}</section><section className="mt-20 flex flex-col items-start justify-between gap-8 border-y hairline py-12 sm:flex-row sm:items-center"><div><p className="eyebrow">The next step</p><h2 className="display-font mt-3 text-3xl font-bold">Look at the engine, not the promise.</h2></div><Link href="/docs" className="button-secondary">Read the method <ArrowRight size={16} /></Link></section><p className="mx-auto mt-16 flex max-w-2xl items-center justify-center gap-2 text-center text-sm text-muted-foreground"><ShieldCheck size={16} className="shrink-0 text-primary" /> Built for teams who would rather catch a secret than explain a breach.</p></div>; }
