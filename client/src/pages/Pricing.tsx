import { ArrowRight, Check, Minus } from "lucide-react";
import { Link } from "wouter";

const tiers = [{ name: "Local", price: "0", detail: "For individual workspaces and trying the engine.", features: ["Live snippet scanner", "Regex + entropy signals", "Masked findings", "No account required"], action: "Run a scan", href: "/scan" }, { name: "Team", price: "29", detail: "For teams that want a consistent merge gate.", features: ["Everything in Local", "Repository history", "Shared findings view", "CI / pre-commit guidance"], action: "Talk to us", href: "/about", featured: true }, { name: "Platform", price: "Custom", detail: "For security teams standardizing secret hygiene.", features: ["Everything in Team", "Custom retention controls", "SAML / SCIM planning", "Implementation support"], action: "Plan the rollout", href: "/about" }];

export default function Pricing() { return <div className="container py-16 sm:py-24"><div className="max-w-3xl"><p className="eyebrow">Pricing / calm by default</p><h1 className="display-font mt-4 text-5xl font-bold leading-[.98] sm:text-7xl">Choose the amount of merge-path you need.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-muted-foreground">Start locally. Add shared visibility when the team needs it. No dark patterns, no surprise motion, no fake customer wall.</p></div><div className="mt-16 grid gap-4 lg:grid-cols-3">{tiers.map(tier => <article key={tier.name} className={`surface relative flex flex-col border p-7 sm:p-8 ${tier.featured ? "border-primary shadow-[0_18px_45px_rgba(207,90,53,.12)]" : "hairline"}`}>{tier.featured && <div className="absolute right-6 top-6 rounded-full bg-primary px-3 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-primary-foreground">most useful</div>}<p className="eyebrow">{tier.name}</p><div className="mt-8 flex items-end gap-1"><span className="display-font text-5xl font-bold">{tier.price === "Custom" ? tier.price : `$${tier.price}`}</span>{tier.price !== "Custom" && <span className="mb-2 text-sm text-muted-foreground">/ user / month</span>}</div><p className="mt-4 min-h-12 text-sm leading-6 text-muted-foreground">{tier.detail}</p><Link href={tier.href} className={`mt-8 w-full ${tier.featured ? "button-primary" : "button-secondary"}`}>{tier.action} <ArrowRight size={15} /></Link><div className="mt-9 border-t hairline pt-6"><p className="eyebrow">Includes</p><ul className="mt-4 space-y-3">{tier.features.map(feature => <li key={feature} className="flex gap-3 text-sm"><Check size={16} className="mt-0.5 shrink-0 text-primary" /><span>{feature}</span></li>)}</ul></div></article>)}</div><div className="mt-12 flex items-center gap-3 text-sm text-muted-foreground"><Minus size={15} className="text-primary" /> Prices are a starting point for the product direction, not a billing flow.</div></div>; }
